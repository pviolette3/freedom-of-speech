<!DOCTYPE html>

<html>
  <head>
    <% include header_files %>
  </head>
  
  <body>
    <% include navbar %>
    <div class="container-fluid max-height no-overflow">
        <div class="row-fluid">
            <h2>Hello, admin!</h2>
            <h4>Here is the info on your room. Click <a href="/chat/<%= id %>/">here</a> to go back.</h4>
            <form name="input" action="/chat/<%=id%>/weights" method="post">
                <input type="text" name="word" placeholder="Word to update:"> <br>
                <div class="form-horizontal">
                    <input type="number" name="weight" placeholder="New weight:">
                    <input type="submit" value="Update word" class="btn">
                </div>
            </form>
        </div>
        <div class="row=fluid max-height-short">
            <h4> Format is <i>Word : Offensiveness Score</i></h4>
            <h6> <i>A score of 10 causes censorship.</i> </h6>
            <ul class="scrollable-short">
              <% for(var word in weights) { %>
                <li> <b><%= word %></b> : <%= weights[word] %> </li> 
              <% } %>
            </ul>
        </div>
    </div>
  </body>

</html>